# Руководство по развертыванию проекта Hotel Management

> **📖 Для быстрого старта см. [QUICK_START.md](./QUICK_START.md)**

## Быстрый старт с Docker Compose

### Предварительные требования

- Docker (версия 20.10 или выше)
- Docker Compose (версия 2.0 или выше)

### Шаги развертывания

1. **Клонируйте репозиторий** (если еще не сделано):
   ```bash
   git clone <repository-url>
   cd hotel-management
   ```

2. **Создайте файл `.env`** на основе `env.example`:
   ```bash
   cp env.example .env
   ```

3. **Отредактируйте `.env`** при необходимости (пароли, порты и т.д.)

4. **Запустите все сервисы**:
   ```bash
   docker-compose up -d
   ```

5. **Проверьте статус сервисов**:
   ```bash
   docker-compose ps
   ```

6. **Просмотрите логи**:
   ```bash
   # Все логи
   docker-compose logs -f
   
   # Логи конкретного сервиса
   docker-compose logs -f backend
   docker-compose logs -f frontend
   docker-compose logs -f postgres
   ```

7. **Заполните базу данных тестовыми данными** (опционально):
   ```bash
   docker-compose exec backend ./gradlew run --args="com.example.scripts.SeedDatabaseKt"
   ```

8. **Откройте приложение**:
   - Frontend: http://localhost:3000
   - Backend API: http://localhost:8080
   - Swagger UI: http://localhost:8080/openapi

### Остановка сервисов

```bash
docker-compose down
```

### Остановка с удалением данных

```bash
docker-compose down -v
```

## 🗄 База данных

### Автоматическое создание

✅ **База данных создается автоматически!**

При запуске backend через Docker:
1. PostgreSQL контейнер автоматически создает базу данных `hotel_db` и пользователя `hotel_admin`
2. Backend автоматически создает все таблицы при первом подключении через Exposed ORM
3. Данные сохраняются в Docker volume и не теряются при перезапуске

**Создаваемые таблицы:**
- `rooms` - номера отеля
- `users` - пользователи системы
- `employees` - сотрудники
- `clients` - клиенты
- `invoices` - счета
- `cleaningschedule` - расписание уборки

### Заполнение тестовыми данными

После первого запуска заполните БД тестовыми данными:

```bash
# С Docker
docker-compose exec backend ./gradlew run --args="com.example.scripts.SeedDatabaseKt"

# Без Docker
cd backend
./gradlew run --args="com.example.scripts.SeedDatabaseKt"
```

Или запустите `SeedDatabase.kt` через IDE.

## Развертывание без Docker

### Backend

1. **Установите PostgreSQL** и создайте базу данных:
   ```sql
   CREATE DATABASE hotel_db;
   CREATE USER hotel_admin WITH PASSWORD 'orf.12014319N';
   GRANT ALL PRIVILEGES ON DATABASE hotel_db TO hotel_admin;
   ```

2. **Настройте переменные окружения** (опционально):
   ```bash
   export DB_URL=jdbc:postgresql://localhost:5432/hotel_db
   export DB_USER=hotel_admin
   export DB_PASSWORD=orf.12014319N
   export DB_DRIVER=org.postgresql.Driver
   ```

3. **Соберите и запустите**:
   ```bash
   cd backend
   ./gradlew build
   ./gradlew run
   ```

   **Примечание:** Таблицы создадутся автоматически при первом запуске!

### Frontend

1. **Установите зависимости**:
   ```bash
   cd frontend
   npm install
   ```

2. **Настройте переменные окружения**:
   Создайте файл `.env`:
   ```
   VITE_API_URL=http://localhost:8080
   ```

3. **Запустите в режиме разработки**:
   ```bash
   npm run dev
   ```

4. **Или соберите для продакшена**:
   ```bash
   npm run build
   npm run preview
   ```

## Заполнение базы данных тестовыми данными

### С Docker

```bash
docker-compose exec backend ./gradlew run --args="com.example.scripts.SeedDatabaseKt"
```

### Без Docker

```bash
cd backend
./gradlew run --args="com.example.scripts.SeedDatabaseKt"
```

Или запустите скрипт напрямую через IDE.

## Переменные окружения

### Backend

- `DB_URL` - URL подключения к базе данных (по умолчанию: `jdbc:postgresql://localhost:5432/hotel_db`)
- `DB_USER` - Имя пользователя БД (по умолчанию: `hotel_admin`)
- `DB_PASSWORD` - Пароль БД (по умолчанию: `orf.12014319N`)
- `DB_DRIVER` - Драйвер БД (по умолчанию: `org.postgresql.Driver`)
- `JWT_SECRET` - Секретный ключ для JWT (по умолчанию: `supersecretkey`)

### Frontend

- `VITE_API_URL` - URL бэкенд API (по умолчанию: `http://localhost:8080`)

## Проблемы и решения

### Проблема: Backend не может подключиться к БД

**Решение:**
1. Убедитесь, что PostgreSQL запущен
2. Проверьте переменные окружения
3. Проверьте логи: `docker-compose logs backend`

### Проблема: Frontend не может подключиться к Backend

**Решение:**
1. Проверьте, что Backend запущен
2. Проверьте переменную `VITE_API_URL`
3. Проверьте CORS настройки в Backend

### Проблема: Порты уже заняты

**Решение:**
Измените порты в `.env` файле:
```
DB_PORT=5433
BACKEND_PORT=8081
FRONTEND_PORT=3001
```

## Обновление приложения

1. **Остановите сервисы**:
   ```bash
   docker-compose down
   ```

2. **Обновите код**:
   ```bash
   git pull
   ```

3. **Пересоберите и запустите**:
   ```bash
   docker-compose up -d --build
   ```

## Резервное копирование базы данных

```bash
# Создать бэкап
docker-compose exec postgres pg_dump -U hotel_admin hotel_db > backup.sql

# Восстановить из бэкапа
docker-compose exec -T postgres psql -U hotel_admin hotel_db < backup.sql
```

## Мониторинг

### Проверка здоровья сервисов

```bash
# Backend
curl http://localhost:8080

# Frontend
curl http://localhost:3000
```

### Просмотр использования ресурсов

```bash
docker stats
```

