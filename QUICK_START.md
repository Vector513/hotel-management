# Быстрый старт - Развертывание проекта Hotel Management

## 🐳 Развертывание с Docker (Рекомендуется)

### Предварительные требования

- **Docker Desktop** установлен и запущен (для Windows)
- **Docker** версии 20.10 или выше
- **Docker Compose** версии 2.0 или выше

⚠️ **Важно:** Убедитесь, что Docker Desktop запущен перед выполнением команд!

### Шаги развертывания

#### 1. Клонирование и подготовка

```bash
# Перейдите в директорию проекта
cd hotel-management

# Создайте файл .env из примера
cp env.example .env
```

#### 2. Настройка переменных окружения (опционально)

Отредактируйте файл `.env` при необходимости:

```env
# Database Configuration
DB_PASSWORD=orf.12014319N
DB_PORT=5432

# Backend Configuration
BACKEND_PORT=8080
JWT_SECRET=supersecretkey

# Frontend Configuration
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:8080
```

#### 3. Запуск всех сервисов

```bash
# Запустить все сервисы в фоновом режиме
docker-compose up -d

# Или с просмотром логов
docker-compose up
```

#### 4. Проверка статуса

```bash
# Проверить статус всех контейнеров
docker-compose ps

# Просмотреть логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f postgres
```

#### 5. Заполнение базы данных тестовыми данными

После того, как все сервисы запустились, заполните БД тестовыми данными:

**Для Windows PowerShell (скопируйте и выполните эту команду целиком):**

```powershell
docker-compose exec -e DB_URL=jdbc:postgresql://postgres:5432/hotel_db -e DB_USER=hotel_admin -e DB_PASSWORD=orf.12014319N -e DB_DRIVER=org.postgresql.Driver backend java -cp app.jar com.example.scripts.SeedDatabaseKt
```

**Для Linux/Mac (bash):**

```bash
docker-compose exec -e DB_URL=jdbc:postgresql://postgres:5432/hotel_db \
  -e DB_USER=hotel_admin \
  -e DB_PASSWORD=orf.12014319N \
  -e DB_DRIVER=org.postgresql.Driver \
  backend java -cp app.jar com.example.scripts.SeedDatabaseKt
```

**Что создаст скрипт:**
- 1 администратор (логин: `admin`, пароль: `admin123`)
- 30 номеров отеля
- 10 работников (логины: `worker1`-`worker10`, пароль: `password123`)
- 50 клиентов (логины: `client1`-`client50`, пароль: `password123`)
- Расписание уборки и счета

📖 **Подробная инструкция:** См. [FILL_DATABASE.md](./FILL_DATABASE.md)

#### 6. Открытие приложения

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8080
- **Swagger UI**: http://localhost:8080/openapi

### Учетные данные по умолчанию

После заполнения БД тестовыми данными:

- **Администратор**: `admin` / `admin123`
- **Клиенты**: `client1`, `client2`, ... / `password123`
- **Работники**: `worker1`, `worker2`, ... / `password123`

---

## 📦 Что происходит при развертывании

### База данных (PostgreSQL)

✅ **Автоматически создается:**
- Контейнер PostgreSQL 15
- База данных `hotel_db`
- Пользователь `hotel_admin`
- Volume для хранения данных (сохраняется между перезапусками)

✅ **Автоматически создаются таблицы:**
- При первом запуске backend автоматически создает все таблицы через Exposed ORM
- Таблицы: `rooms`, `users`, `employees`, `clients`, `invoices`, `cleaning_schedules`

### Backend

✅ **Автоматически:**
- Собирается JAR файл
- Подключается к PostgreSQL
- Создает схему БД (таблицы)
- Запускается на порту 8080

### Frontend

✅ **Автоматически:**
- Собирается production build
- Настраивается Nginx
- Запускается на порту 3000

---

## 🛠 Управление сервисами

### Остановка

```bash
# Остановить все сервисы
docker-compose down

# Остановить и удалить volumes (удалит данные БД!)
docker-compose down -v
```

### Перезапуск

```bash
# Перезапустить все сервисы
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart backend
```

### Обновление

```bash
# Остановить сервисы
docker-compose down

# Пересобрать образы
docker-compose build --no-cache

# Запустить заново
docker-compose up -d
```

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100

# Логи конкретного сервиса
docker-compose logs -f backend
```

---

## 🔧 Развертывание без Docker

### Backend

1. **Установите PostgreSQL** и создайте БД:

```sql
CREATE DATABASE hotel_db;
CREATE USER hotel_admin WITH PASSWORD 'orf.12014319N';
GRANT ALL PRIVILEGES ON DATABASE hotel_db TO hotel_admin;
```

2. **Настройте переменные окружения** (опционально):

```bash
export DB_URL=jdbc:postgresql://localhost:5432/hotel_db
export DB_USER=hotel_admin
export DB_PASSWORD=orf.12014319N
export DB_DRIVER=org.postgresql.Driver
```

3. **Соберите и запустите**:

```bash
cd backend
./gradlew build
./gradlew run
```

При первом запуске таблицы создадутся автоматически.

### Frontend

1. **Установите зависимости**:

```bash
cd frontend
npm install
```

2. **Создайте `.env` файл**:

```env
VITE_API_URL=http://localhost:8080
```

3. **Запустите**:

```bash
# Режим разработки
npm run dev

# Production build
npm run build
npm run preview
```

---

## 🗄 База данных

### Автоматическое создание таблиц

✅ **Таблицы создаются автоматически** при первом запуске backend через Exposed ORM:
- `rooms` - номера отеля
- `users` - пользователи системы
- `employees` - сотрудники
- `clients` - клиенты
- `invoices` - счета
- `cleaning_schedules` - расписание уборки

### Резервное копирование

```bash
# Создать бэкап
docker-compose exec postgres pg_dump -U hotel_admin hotel_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из бэкапа
docker-compose exec -T postgres psql -U hotel_admin hotel_db < backup.sql
```

### Подключение к БД извне

```bash
# Через Docker
docker-compose exec postgres psql -U hotel_admin -d hotel_db

# Или напрямую (если порт проброшен)
psql -h localhost -p 5432 -U hotel_admin -d hotel_db
```

---

## 🐛 Решение проблем

### Docker Desktop не запущен

**Ошибка:** `unable to get image: error during connect: open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified`

**Решение:**
1. Запустите **Docker Desktop** из меню Пуск или из системного трея
2. Дождитесь полного запуска (иконка Docker в трее должна быть активна)
3. Проверьте статус: `docker ps` (должен выполниться без ошибок)
4. Повторите команду `docker-compose up -d`

### Backend не может подключиться к БД

1. Проверьте, что PostgreSQL запущен:
   ```bash
   docker-compose ps postgres
   ```

2. Проверьте логи:
   ```bash
   docker-compose logs postgres
   docker-compose logs backend
   ```

3. Проверьте переменные окружения в `.env`

### Frontend не может подключиться к Backend

1. Проверьте, что Backend запущен:
   ```bash
   curl http://localhost:8080
   ```

2. Проверьте переменную `VITE_API_URL` в `.env`

3. Пересоберите frontend:
   ```bash
   docker-compose up -d --build frontend
   ```

### Порты уже заняты

Измените порты в `.env`:
```env
DB_PORT=5433
BACKEND_PORT=8081
FRONTEND_PORT=3001
```

### Очистка и перезапуск

```bash
# Остановить все
docker-compose down

# Удалить volumes (удалит данные БД!)
docker-compose down -v

# Пересобрать и запустить
docker-compose up -d --build
```

---

## 📝 Полезные команды

```bash
# Просмотр использования ресурсов
docker stats

# Войти в контейнер
docker-compose exec backend bash
docker-compose exec frontend sh
docker-compose exec postgres psql -U hotel_admin -d hotel_db

# Проверка здоровья сервисов
curl http://localhost:8080
curl http://localhost:3000

# Очистка неиспользуемых образов
docker system prune -a
```

---

## ✅ Проверка работоспособности

1. **Проверьте, что все контейнеры запущены**:
   ```bash
   docker-compose ps
   ```
   Все сервисы должны быть в статусе `Up`

2. **Проверьте доступность сервисов**:
   - Backend: http://localhost:8080 (должен вернуть "Hello World!")
   - Frontend: http://localhost:3000 (должна открыться страница логина)

3. **Проверьте логи на ошибки**:
   ```bash
   docker-compose logs | grep -i error
   ```

4. **Заполните БД тестовыми данными** (если еще не сделано)

5. **Войдите в систему** с учетными данными администратора

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение должно быть полностью развернуто и готово к использованию!

Подробная документация: [DEPLOYMENT.md](./DEPLOYMENT.md)

