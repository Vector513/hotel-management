# Database Configuration
# Пароль для PostgreSQL базы данных
DB_PASSWORD=orf.12014319N

# Порт для PostgreSQL (по умолчанию: 5432)
DB_PORT=5432

# Backend Configuration
# Порт для backend API (по умолчанию: 8080)
BACKEND_PORT=8080

# Секретный ключ для JWT токенов (измените на свой в продакшене!)
JWT_SECRET=supersecretkey

# Frontend Configuration
# Порт для frontend (по умолчанию: 3000)
FRONTEND_PORT=3000

# URL backend API для frontend (по умолчанию: http://localhost:8080)
# Для Docker используйте: http://localhost:8080
# Для продакшена укажите реальный URL вашего backend
VITE_API_URL=http://localhost:8080

